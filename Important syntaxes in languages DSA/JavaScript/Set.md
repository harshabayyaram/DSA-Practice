## SET

```java
Declaration --> let sets = new Set()
adding --> sets.add(arr[i]);
size --> sets.size;
Checking for element --> sets.has(1);
Clearing --> sets.clear();

looping --> 
for(let num of sets){
   res[i] = num;
   i++;
}
```
